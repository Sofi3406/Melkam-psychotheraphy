{
  "name": "backend",
  "version": "1.0.0",
  "description": "- Each therapist cannot have overlapping `AvailabilitySlot`s.  \r   → Enforced with `@@unique([therapistId, startsAt, endsAt])`.",
  "main": "index.js",
  "scripts": {
    
    "dev": "ts-node src/index.ts",
    "test": "cross-env NODE_ENV=test jest --runInBand",
    "migrate:test": "cross-env NODE_ENV=test npx prisma migrate deploy --schema=prisma/schema.test.prisma",
   "worker": "ts-node src/worker.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:studio": "prisma studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/jest": "^30.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.3.0",
    "@types/supertest": "^6.0.3",
    "cross-env": "^10.0.0",
    "dotenv-cli": "^10.0.0",
    "jest": "^30.1.1",
    "prisma": "^6.15.0",
    "supertest": "^7.1.4",
    "ts-jest": "^29.4.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "bullmq": "^5.58.4",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "ioredis": "^5.7.0",
    "jsonwebtoken": "^9.0.2"
  }
}
